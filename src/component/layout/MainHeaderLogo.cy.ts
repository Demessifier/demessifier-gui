import MainHeaderLogo from "./MainHeaderLogo.vue";

describe("MainHeaderLogo component", () => {
  it(`Renders`, () => {
    cy.mount(MainHeaderLogo);
    expect(false).to.be.true; // TODO: write tests
  });
});
